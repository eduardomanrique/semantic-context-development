# Domains

This directory contains domain-oriented semantic definitions.

A domain file should describe stable business meaning, core entities, relevant concepts, permissions, and rules that belong to a coherent area of the system.

Examples:
- orders
- payments
- users
- documents
- approvals

Each domain file should prefer semantic meaning over implementation detail, but must still preserve compatibility-relevant structures when they affect observable behavior.
